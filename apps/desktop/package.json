{
  "name": "desktop",
  "version": "0.13.40",
  "private": true,
  "type": "module",
  "description": "Airwave Desktop — an Electrobun tray-only supervisor that runs embedded Postgres + the Airwave server + admin + tv-web on local ports, next to Plex. The browser is the UI. See .plans/desktop-server.md.",
  "scripts": {
    "start": "pnpm prebuild && electrobun dev",
    "dev": "pnpm prebuild && electrobun dev",
    "prebuild": "turbo -F web build -F tv-web build -F @airwave/desktop-setup build && pnpm -F server build:standalone && pnpm run build:pg-launcher",
    "build:pg-launcher": "bun scripts/build-pg.ts",
    "build": "electrobun build",
    "build:stable": "electrobun build --env=stable",
    "build:canary": "electrobun build --env=canary",
    "build:win-installer": "bun scripts/build-win-installer.ts",
    "build:mac-signed": "bun scripts/build-mac-signed.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "electrobun": "^2.0.1",
    "embedded-postgres": "18.4.0-beta.17"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
